# Режим работы: service_account | public_csv | mock (по умолчанию)
# Чтобы видеть данные из Google-таблиц — поставьте service_account
MODE=mock

# --- Google Sheets (MODE=service_account) ---
GOOGLE_SERVICE_ACCOUNT_JSON=

# Единая таблица (если одна таблица для всего):
GOOGLE_SHEETS_SPREADSHEET_ID=

# Две отдельные таблицы (воронка + связки):
GOOGLE_SHEETS_SALES_FUNNEL_ID=1Ibc0FSJg3gFTfElZQ6jbAaU2-F7wf89Wf-gHp-klL58
GOOGLE_SHEETS_CONNECTIONS_ID=1eUmK264I8OMDs69FlIuDiGXkE9B4gO2FnywxiWNnoDo

# Имена листов: Сделки — воронка, Компании — связки (если листы называются иначе)
SALES_FUNNEL_SHEET_NAME=Сделки
CONNECTIONS_SHEET_NAME=Компании

# Сотрудники, исключённые из списка (через запятую). По умолчанию — Корецкий
# EXCLUDED_FROM_EMPLOYEES=Корецкий

# JSON credentials Service Account в одну строку
# Пример: {"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}

# --- Загрузка фото (Vercel Blob) ---
# Без токена — локально фото хранятся как base64 в mock. На Vercel нужен токен.
BLOB_READ_WRITE_TOKEN=

# --- Public CSV (MODE=public_csv) ---
PUBLIC_CSV_COMPANIES_URL=
PUBLIC_CSV_INVOICES_URL=
PUBLIC_CSV_ABOUT_URL=
